name: mirror
description: Mirror is a lightweight static reflection library for Dart, using code generation.
  It is designed as a modern alternative to `reflectable`.

version: 0.1.0
resolution: workspace
repository: https://github.com/alessandro-amos/mirror.dart

environment:
  sdk: ">=3.8.2 <4.0.0"

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
